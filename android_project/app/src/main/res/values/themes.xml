<resources>
    <!-- AppCompat-based theme required because MainActivity extends AppCompatActivity
         (needed for AppCompatDelegate locales + BiometricPrompt which requires FragmentActivity). -->
    <style name="Theme.FinanceTracker" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
